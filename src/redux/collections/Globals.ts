interface Globals {
    ready: boolean
    localDeviceId?: string
    localUserId?: string
    // Stage related
    stageId?: string
    stageMemberId?: string
    groupId?: string
    localStageDeviceId?: string
}

export default Globals
